// import { motion } from "framer-motion";
// import React, { ReactNode } from "react";

const TransitionAnimation = () => {
  return (
    <>
    {/* {props.children}
    <motion.div
    className="page-in fixed top-0 left-0 w-full h-full bg-dark-primary z-50 origin-bottom"
      initial={{ scaleY: 0 }}
      animate={{ scaleY: 0 }}
      exit={{ scaleY: 1 }}.
      transition={{ duration: 0.5, ease: [0.22, 1, 0.36, 1] }}
    />

     <motion.div
    className="page-out fixed top-0 left-0 w-full h-full bg-dark-primary z-50 origin-top"
      initial={{ scaleY: 1 }}
      animate={{ scaleY: 0 }}
      exit={{ scaleY: 0 }}
      transition={{ duration: 0.5, ease: [0.22, 1, 0.36, 1] }}
    /> */}
      
 
    </>
  );
}

export default TransitionAnimation;